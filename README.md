# localvibe-web

Public assets for LocalVibe — landing page and extension download.

- Landing page hosted via GitHub Pages
- Extension zip attached to each release
- `version.json` used by the CLI for update checks
